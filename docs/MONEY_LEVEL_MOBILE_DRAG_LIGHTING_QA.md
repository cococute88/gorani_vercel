# Money Level: House lighting / mobile drag follow-up

1. **Start main:** `b911709cc22a572537cf857fa3428be424ec79b6`. PR #231 merged at `2026-09-13T11:20:26Z`; fetch/pull/status clean before analysis. Workspace: `C:\gv\gorani_vercel` (the alternative path in the request does not exist). Branch: `codex/money-level-mobile-drag-lighting`.
2. **Previous lighting:** shared time/weather saturation and RGB multiplication for House/Camp/Statue; separate exposure only. House and Statue also shared an SVG matrix filter.
3. **Yellow cast:** Evening added sepia `.06` and suppressed blue to `.78`. Night dimmed daylight paint but retained yellow-green material hues (`hue-rotate(0deg)`). A filter's existence did not establish visual acceptance.
4. **Evening:** saturation `.82`, sepia `0`, hue `-12deg`, RGB `[1.08,.94,.97]`. Reduce yellow while putting timber/camp in pink sunset ambient. Exposure `.82` unchanged. Values were visually compared with the edited reference, not copied from Clip Studio HSV.
5. **Night:** saturation `.78`, hue `14deg`, RGB `[.70,.88,1.16]`, exposure `.67` unchanged. Roof identity and detail remain; cool ambient reduces daylight yellow-green. Compare Sunny and Thunderstorm with the Night reference's direction.
6. **Morning:** visually reviewed; mild existing correction is natural. Morning and Day Sunny resolver values unchanged.
7. **Cloudy:** additional saturation multiplier `.80` (20% reduction). Evening final `.656`; Night final `.624`.
8. **Thunderstorm:** additional saturation multiplier `.76` (24% reduction). Evening final `.623`; Night final `.593`. Existing darker exposure and cool weather RGB remain. Rain multiplier `.85`.
9. **Statues:** separate SVG filter ID/matrix; all 16 existing Statue CSS filters and RGB matrices exactly preserved. Position/scale/assets unchanged. Character grading and all 16 background artworks unchanged.
10. **Visual QA:** scene plus separate Brokerage/Tax captures reviewed for Morning Sunny, Day Sunny, Evening Sunny, Evening Cloudy, Evening Rain, Night Sunny, Night Cloudy, Night Thunderstorm. Night roof is cool green, not blue paint; Evening timber is pink/warm without excess yellow; cloudy/storm materials visibly less saturated. Production Preview build captures include both statues and existing ambient layers. Local artifacts: `%TEMP%\money-level-follow-up-production-qa`.
11. **Mobile failure analysis:** confirmed code defect: at >=12px movement during the 320ms wait, the previous handler cleared the timer/session without transferring ownership to pan or page. Small tests with 6px jitter could not detect this path. The old parent pan handler already checked for a pending drag session, so premature parent pan acquisition was not established. Hitboxes matched initial sprites in local Chrome; they were narrow and had less vertical finger padding. **The unique cause on the user's Android is not confirmed without its event trace.** Changes address these verified weak points and expose the remaining real-device uncertainty.
12. **Event flow:** captured trusted Chrome events: pointerdown → touchstart → pointermove → touchmove → pointerup → touchend. Target press stops bubbling to parent pan and captures immediately. Jitter keeps the same timer. Acquired drag exclusively updates its controller/camera. Native early vertical scroll produces pointercancel; cancellation/lost capture clears timer/feedback/capture and uses the current world position.
13. **Touch/capture:** capture starts on pointerdown. Target `touch-action: pan-y pinch-zoom` permits native early vertical scroll. A target-scoped `touchmove` listener with `{passive:false}` prevents default while a pending press/active drag owns the single touch; intentional early movement releases ownership before that listener. Pointer move remains non-passive. Capture alone cannot veto browser viewport gestures ([Pointer Events specification](https://www.w3.org/TR/pointerevents3/)). Chrome sequence ordering/default prevention tested; actual Android still requires confirmation.
14. **Threshold/tolerance:** 280ms / 16px radial maximum displacement from initial press. Timer does not reset on tolerated jitter. No acquisition at 100ms. Delayed 5/10/15px movement acquires both characters.
15. **Hitboxes:** mobile Gorani `48×66` → `48×82`; Daramji `46×62` → `46×78`. Bottom `-8` → `-16px`, adding 8px above and below. Width unchanged to avoid introducing horizontal overlap. Sprite size, desktop bounds and labels unchanged. Actual elementFromPoint targets verified.
16. **Ownership:** pending character press blocks parent pan. Over-tolerance horizontal intent explicitly transfers the same pointer/start coordinates to FOREST_PAN. Early vertical intent releases to PAGE. Once CHARACTER_DRAG activates, pan cannot steal it. Secondary pointers cannot start another drag. Query `interactionDebug=1` shows owner, timer/elapsed/threshold, pointer type, displacement/tolerance, capture, cancel count, cameraX, grabbed character and timestamped pointer/touch event tail. UI and tracing enabled only by server-approved dev/Preview flag.
17. **Simulated Android:** trusted Chrome CDP touch, Android UA, 390px, radius 9px, 0/5/10/15px jitter for both actors, delayed movement, acquired vertical movement without scrolling, release, early horizontal pan, early vertical native scroll including preceding 5px jitter, cancellation before/after timer. Also existing 390px edge carry/Activity drops, 768px tablet and 768/1320px desktop browser suite. **SIMULATED TOUCH PASS.** Final follow-up suite also passed against optimized production Preview build.
18. **Real device:** **REAL DEVICE NOT RUN**. No physical Android device was available. User confirmation required before any merge.
19. **Forest pan:** trusted horizontal touch both directions, native blank-scene vertical scroll, camera bounds, background coverage, no body horizontal overflow, screen/world round trip, edge carry at both sides all pass.
20. **Activities:** Gorani/Daramji Fishing, single Fishing Slot replacement → Pond Watch; Bench replacement → nearby safe grass; global Ceremony exchanges/occupancy/exits/none availability and existing activity/controller tests pass. Desktop Bench/Dock mouse drops pass.
21. **Checks:** all existing Money Level scripts plus new `check:money-level-house-lighting` and `check:money-level-follow-up-browser`; typecheck, lint, production build and diff whitespace checks pass. Local finance backend has no service account credentials, so scene QA uses the existing Preview fixture; live portfolio backend QA is outside this change.
22. **Commit:** focused follow-up commit covering the two requested axes, instrumentation, regression checks and this QA record. Exact hash in completion report/PR history.
23. **Draft PR / deployment:** URLs in completion report. Vercel Git integration creates Preview from pushed branch; automatic merge is not enabled.
24. **User Android steps:** open the exact Preview `/money-level?time=night&weather=sunny&interactionDebug=1`; reload to use this deployment. Hold each visible character ~0.3s, observe slight scale/shadow feedback and `CHARACTER_DRAG`, move both horizontally and vertically, carry to each edge to pan the camera, release on Bench/Dock/Statue activity zones, verify world placement/activity and recovery. Swipe blank forest to pan; perform an early vertical character swipe to scroll. Repeat with `weather=thunderstorm` and `time=evening&weather=sunny` to assess House/Camp lighting. If grabbing fails, capture the debug event tail including target ID, displacement, timer, capture and pointercancel count.

Stop after delivery and wait for user real-device confirmation. Do not merge.
